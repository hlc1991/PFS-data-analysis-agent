# Optional deep-learning modelling dependencies (M2).
# Do not add this file to the core requirements: CUDA wheels and their runtime
# selection are platform-specific. For a matching CUDA build, follow
# https://pytorch.org/get-started/locally/ before installing this file.
torch>=2.2.0
