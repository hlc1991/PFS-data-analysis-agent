# Build-only dependencies. Runtime dependencies remain in requirements.txt.
# Pin PyInstaller so Windows/macOS GitHub runners produce comparable layouts.
pyinstaller==6.21.0
Pillow>=10.0.0         # installer/make_ico.py — PNG to multi-size ICO conversion
