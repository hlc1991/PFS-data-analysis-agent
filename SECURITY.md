# PFS 安全策略

## 当前支持边界

PFS 当前以本地桌面正式版交付，尚未声明任何生产级 SLA。每个版本都应明确区分静态检查、离线契约测试、真实服务测试、部署验证和线上验收。

## 报告安全问题

请优先使用 GitHub Private vulnerability reporting（如仓库已启用）或仓库维护者指定的私密渠道报告；不要在公开 Issue 中粘贴 API Key、用户数据、凭据、数据库连接串或可利用漏洞细节。

报告应尽量包含：

- 受影响的版本、提交或部署环境；
- 可复现步骤和最小化样例；
- 影响范围、是否涉及数据泄露或权限绕过；
- 临时缓解措施（如有）。

## 运行安全原则

- 外部网页、MCP 返回、工具结果和模型输出都视为不可信输入；
- 工具必须经过参数校验、allowlist、工作区边界和预算边界；
- 写入和外部副作用必须有明确意图、权限、幂等和审计；
- 日志与错误响应不得泄露密钥、完整连接串或敏感数据；
- 上传数据、生成物、SQLite 状态和本地密钥不进入版本控制。
